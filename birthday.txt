<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <title>Birthday Celebration 🎉</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
        }

        /* Animated background particles */
        .particle {
            position: absolute;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            pointer-events: none;
            animation: float 8s ease-in-out infinite;
        }

        .particle:nth-child(odd) {
            background: linear-gradient(45deg, #ff6b6b, #feca57);
            animation: floatColorful 6s ease-in-out infinite;
        }

        .particle:nth-child(even) {
            background: linear-gradient(45deg, #48dbfb, #ff9ff3);
            animation: floatSpiral 7s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); opacity: 0.8; }
            25% { transform: translateY(-30px) rotate(90deg); opacity: 1; }
            50% { transform: translateY(-60px) rotate(180deg); opacity: 0.6; }
            75% { transform: translateY(-30px) rotate(270deg); opacity: 1; }
        }

        @keyframes floatColorful {
            0%, 100% { transform: translateY(0px) rotate(0deg) scale(1); opacity: 0.7; }
            33% { transform: translateY(-40px) rotate(120deg) scale(1.2); opacity: 1; }
            66% { transform: translateY(-20px) rotate(240deg) scale(0.8); opacity: 0.9; }
        }

        @keyframes floatSpiral {
            0%, 100% { transform: translateX(0px) translateY(0px) rotate(0deg) scale(1); opacity: 0.6; }
            25% { transform: translateX(30px) translateY(-40px) rotate(90deg) scale(1.3); opacity: 1; }
            50% { transform: translateX(0px) translateY(-80px) rotate(180deg) scale(0.7); opacity: 0.8; }
            75% { transform: translateX(-30px) translateY(-40px) rotate(270deg) scale(1.1); opacity: 0.9; }
        }

        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
            position: relative;
            z-index: 10;
        }

        .celebration-header {
            text-align: center;
            margin-bottom: 50px;
            animation: slideDown 1s ease-out;
        }

        @keyframes slideDown {
            from { transform: translateY(-50px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        .celebration-header h1 {
            font-size: 4rem;
            font-weight: 800;
            color: #fff;
            text-shadow: 0 4px 8px rgba(0,0,0,0.3);
            margin-bottom: 20px;
            background: linear-gradient(45deg, #ff6b6b, #feca57, #48dbfb, #ff9ff3, #54a0ff, #5f27cd);
            background-size: 600% 600%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: gradientShift 2s ease-in-out infinite, textGlow 3s ease-in-out infinite alternate;
        }

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            25% { background-position: 100% 25%; }
            50% { background-position: 100% 100%; }
            75% { background-position: 0% 75%; }
            100% { background-position: 0% 50%; }
        }

        @keyframes textGlow {
            0% { filter: drop-shadow(0 0 10px rgba(255, 107, 107, 0.8)); }
            33% { filter: drop-shadow(0 0 20px rgba(254, 202, 87, 0.8)); }
            66% { filter: drop-shadow(0 0 15px rgba(72, 219, 251, 0.8)); }
            100% { filter: drop-shadow(0 0 25px rgba(255, 159, 243, 0.8)); }
        }

        .celebration-header .emoji {
            font-size: 3rem;
            animation: bounce 1.5s infinite, rotate 4s linear infinite;
            display: inline-block;
            margin: 0 10px;
        }

        .celebration-header .emoji:nth-child(odd) {
            animation: bounce 1.5s infinite, wiggle 2s ease-in-out infinite;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0) scale(1); }
            40% { transform: translateY(-30px) scale(1.1); }
            60% { transform: translateY(-15px) scale(1.05); }
        }

        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        @keyframes wiggle {
            0%, 100% { transform: rotate(0deg) scale(1); }
            25% { transform: rotate(-15deg) scale(1.1); }
            75% { transform: rotate(15deg) scale(0.9); }
        }

        .timer-container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            border-radius: 25px;
            padding: 40px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.3);
            border: 1px solid rgba(255, 255, 255, 0.2);
            animation: slideUp 1s ease-out 0.5s both, containerFloat 6s ease-in-out infinite;
        }

        @keyframes containerFloat {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            25% { transform: translateY(-10px) rotate(0.5deg); }
            50% { transform: translateY(-15px) rotate(0deg); }
            75% { transform: translateY(-5px) rotate(-0.5deg); }
        }

        @keyframes slideUp {
            from { transform: translateY(50px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        .timer-display {
            display: flex;
            gap: 30px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .time-unit {
            display: flex;
            flex-direction: column;
            align-items: center;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 20px;
            min-width: 100px;
            border: 1px solid rgba(255, 255, 255, 0.3);
            transition: all 0.3s ease;
            animation: pulse 2s infinite, shimmer 4s ease-in-out infinite;
            position: relative;
            overflow: hidden;
        }

        .time-unit::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(255,255,255,0.1), transparent);
            transform: rotate(45deg);
            animation: sweep 3s linear infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); box-shadow: 0 0 20px rgba(255, 255, 255, 0.2); }
            50% { transform: scale(1.05); box-shadow: 0 0 30px rgba(255, 255, 255, 0.4); }
        }

        @keyframes shimmer {
            0%, 100% { border-color: rgba(255, 255, 255, 0.3); }
            25% { border-color: rgba(255, 107, 107, 0.6); }
            50% { border-color: rgba(254, 202, 87, 0.6); }
            75% { border-color: rgba(72, 219, 251, 0.6); }
        }

        @keyframes sweep {
            0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
            100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
        }

        /* Background Decorations */
        .bg-decoration {
            position: absolute;
            pointer-events: none;
            z-index: 1;
        }

        .balloon {
            width: 30px;
            height: 40px;
            border-radius: 50% 50% 50% 50% / 60% 60% 40% 40%;
            animation: balloonFloat 8s ease-in-out infinite;
        }

        .balloon::after {
            content: '';
            position: absolute;
            width: 2px;
            height: 50px;
            background: rgba(255,255,255,0.3);
            left: 50%;
            bottom: -50px;
            transform: translateX(-50%);
        }

        .balloon.red { background: linear-gradient(45deg, #ff6b6b, #ff5252); }
        .balloon.yellow { background: linear-gradient(45deg, #feca57, #ff9f43); }
        .balloon.blue { background: linear-gradient(45deg, #48dbfb, #0abde3); }
        .balloon.pink { background: linear-gradient(45deg, #ff9ff3, #f368e0); }

        @keyframes balloonFloat {
            0%, 100% { transform: translateY(0px) rotate(-2deg); }
            25% { transform: translateY(-20px) rotate(2deg); }
            50% { transform: translateY(-35px) rotate(-1deg); }
            75% { transform: translateY(-15px) rotate(1deg); }
        }

        .star {
            width: 0;
            height: 0;
            border-left: 8px solid transparent;
            border-right: 8px solid transparent;
            border-bottom: 6px solid #feca57;
            position: relative;
            animation: starTwinkle 3s ease-in-out infinite;
        }

        .star::before {
            content: '';
            position: absolute;
            left: -8px;
            top: -4px;
            width: 0;
            height: 0;
            border-left: 8px solid transparent;
            border-right: 8px solid transparent;
            border-top: 6px solid #feca57;
        }

        @keyframes starTwinkle {
            0%, 100% { opacity: 0.6; transform: scale(1) rotate(0deg); }
            50% { opacity: 1; transform: scale(1.2) rotate(180deg); }
        }

        .confetti {
            width: 8px;
            height: 8px;
            background: #ff6b6b;
            animation: confettiFall 4s linear infinite;
        }

        .confetti:nth-child(odd) { 
            background: #feca57;
            animation-delay: -1s;
            animation-duration: 5s;
        }
        .confetti:nth-child(3n) { 
            background: #48dbfb;
            animation-delay: -2s;
            animation-duration: 3.5s;
        }
        .confetti:nth-child(4n) { 
            background: #ff9ff3;
            animation-delay: -0.5s;
            animation-duration: 4.5s;
        }

        @keyframes confettiFall {
            0% { transform: translateY(-100vh) rotate(0deg); opacity: 1; }
            100% { transform: translateY(100vh) rotate(720deg); opacity: 0; }
        }

        .gift-box {
            width: 25px;
            height: 25px;
            background: linear-gradient(45deg, #ff6b6b, #ff5252);
            position: relative;
            animation: giftBounce 6s ease-in-out infinite;
        }

        .gift-box::before {
            content: '';
            position: absolute;
            top: -3px;
            left: 10px;
            width: 5px;
            height: 31px;
            background: #feca57;
        }

        .gift-box::after {
            content: '';
            position: absolute;
            top: 10px;
            left: -3px;
            width: 31px;
            height: 5px;
            background: #feca57;
        }

        @keyframes giftBounce {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            25% { transform: translateY(-15px) rotate(5deg); }
            50% { transform: translateY(-25px) rotate(-5deg); }
            75% { transform: translateY(-10px) rotate(3deg); }
        }

        .heart {
            width: 20px;
            height: 18px;
            background: #ff9ff3;
            position: relative;
            animation: heartBeat 2s ease-in-out infinite;
        }

        .heart::before,
        .heart::after {
            content: '';
            width: 26px;
            height: 40px;
            position: absolute;
            background: #ff9ff3;
            border-radius: 20px 20px 0 0;
        }

        .heart::before {
            left: 20px;
            transform: rotate(-45deg);
            transform-origin: 0 100%;
        }

        .heart::after {
            left: 0;
            transform: rotate(45deg);
            transform-origin: 100% 100%;
        }

        @keyframes heartBeat {
            0%, 100% { transform: scale(1); }
            25%, 75% { transform: scale(1.1); }
            50% { transform: scale(1.2); }
        }

        .time-unit:hover {
            transform: translateY(-10px) rotate(5deg) scale(1.1);
            box-shadow: 0 15px 35px rgba(0,0,0,0.3);
            background: rgba(255, 255, 255, 0.2);
            border-color: rgba(255, 255, 255, 0.6);
        }

        .time-number {
            font-size: 3rem;
            font-weight: 700;
            color: #fff;
            text-shadow: 0 2px 4px rgba(0,0,0,0.3);
            margin-bottom: 10px;
            animation: numberPulse 1s ease-in-out infinite alternate;
            position: relative;
            z-index: 2;
        }

        @keyframes numberPulse {
            0% { transform: scale(1); text-shadow: 0 2px 4px rgba(0,0,0,0.3); }
            100% { transform: scale(1.1); text-shadow: 0 4px 8px rgba(255,255,255,0.5), 0 0 20px rgba(255,255,255,0.3); }
        }

        .time-label {
            font-size: 1rem;
            font-weight: 400;
            color: rgba(255, 255, 255, 0.8);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .birthday-message {
            text-align: center;
            margin-top: 40px;
            animation: fadeIn 1s ease-out 1s both;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .birthday-message h2 {
            font-size: 2rem;
            font-weight: 600;
            color: #fff;
            text-shadow: 0 2px 4px rgba(0,0,0,0.3);
            margin-bottom: 20px;
        }

        .birthday-message p {
            font-size: 1.2rem;
            font-weight: 300;
            color: rgba(255, 255, 255, 0.9);
            max-width: 600px;
            line-height: 1.6;
        }

        .celebration-started {
            text-align: center;
            display: none;
        }

        .celebration-started h1 {
            font-size: 5rem;
            font-weight: 800;
            color: #fff;
            text-shadow: 0 4px 8px rgba(0,0,0,0.3);
            margin-bottom: 30px;
            animation: celebrate 1s ease-out;
        }

        @keyframes celebrate {
            0% { transform: scale(0.5); opacity: 0; }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); opacity: 1; }
        }

        .fireworks {
            position: absolute;
            width: 4px;
            height: 4px;
            border-radius: 50%;
            animation: firework 2s ease-out infinite;
        }

        .firework-burst {
            position: absolute;
            width: 6px;
            height: 6px;
            border-radius: 50%;
            animation: fireworkBurst 3s ease-out;
        }

        .firework-trail {
            position: absolute;
            width: 2px;
            height: 20px;
            border-radius: 50%;
            animation: fireworkTrail 1.5s ease-out;
        }

        @keyframes firework {
            0% {
                transform: scale(0);
                opacity: 1;
            }
            100% {
                transform: scale(10);
                opacity: 0;
            }
        }

        @keyframes fireworkBurst {
            0% {
                transform: scale(0) rotate(0deg);
                opacity: 1;
            }
            50% {
                transform: scale(8) rotate(180deg);
                opacity: 0.8;
            }
            100% {
                transform: scale(15) rotate(360deg);
                opacity: 0;
            }
        }

        @keyframes fireworkTrail {
            0% {
                transform: translateY(100px) scale(1);
                opacity: 1;
            }
            100% {
                transform: translateY(-150px) scale(0.3);
                opacity: 0;
            }
        }

        @media (max-width: 768px) {
            .celebration-header h1 {
                font-size: 2.5rem;
            }
            .timer-display {
                gap: 15px;
            }
            .time-unit {
                min-width: 80px;
                padding: 15px;
            }
            .time-number {
                font-size: 2rem;
            }
            .birthday-message h2 {
                font-size: 1.5rem;
            }
            .birthday-message p {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="celebration-header">
            <span class="emoji">🎉</span>
            <h1>Birthday Countdown</h1>
            <span class="emoji">🎂</span>
        </div>

        <div class="timer-container" id="timerContainer">
            <div class="timer-display">
                <div class="time-unit">
                    <div class="time-number" id="days">00</div>
                    <div class="time-label">Days</div>
                </div>
                <div class="time-unit">
                    <div class="time-number" id="hours">00</div>
                    <div class="time-label">Hours</div>
                </div>
                <div class="time-unit">
                    <div class="time-number" id="minutes">00</div>
                    <div class="time-label">Minutes</div>
                </div>
                <div class="time-unit">
                    <div class="time-number" id="seconds">00</div>
                    <div class="time-label">Seconds</div>
                </div>
            </div>
        </div>

        <div class="birthday-message">
            <h2>🎈 Get Ready for the Celebration! 🎈</h2>
            <p>The big day is almost here! Every second brings us closer to an amazing birthday celebration filled with joy, laughter, and unforgettable memories.</p>
        </div>

        <div class="celebration-started" id="celebrationStarted">
            <h1>🎉 Happy Birthday Ishika! 🎉</h1>
            <p style="font-size: 1.5rem; color: #fff; margin-top: 20px;">The celebration has begun! 🎂🎈</p>
        </div>
    </div>

    <script>
        // Create background decorations
        function createDecorations() {
            // Create balloons
            const balloonColors = ['red', 'yellow', 'blue', 'pink'];
            for (let i = 0; i < 8; i++) {
                const balloon = document.createElement('div');
                balloon.className = `bg-decoration balloon ${balloonColors[i % 4]}`;
                balloon.style.left = Math.random() * 100 + '%';
                balloon.style.top = Math.random() * 80 + 10 + '%';
                balloon.style.animationDelay = Math.random() * 8 + 's';
                balloon.style.animationDuration = (Math.random() * 4 + 6) + 's';
                document.body.appendChild(balloon);
            }

            // Create stars
            for (let i = 0; i < 15; i++) {
                const star = document.createElement('div');
                star.className = 'bg-decoration star';
                star.style.left = Math.random() * 100 + '%';
                star.style.top = Math.random() * 100 + '%';
                star.style.animationDelay = Math.random() * 3 + 's';
                star.style.animationDuration = (Math.random() * 2 + 2) + 's';
                document.body.appendChild(star);
            }

            // Create falling confetti
            for (let i = 0; i < 20; i++) {
                const confetti = document.createElement('div');
                confetti.className = 'bg-decoration confetti';
                confetti.style.left = Math.random() * 100 + '%';
                confetti.style.animationDelay = Math.random() * 4 + 's';
                document.body.appendChild(confetti);
            }

            // Create gift boxes
            for (let i = 0; i < 6; i++) {
                const gift = document.createElement('div');
                gift.className = 'bg-decoration gift-box';
                gift.style.left = Math.random() * 100 + '%';
                gift.style.top = Math.random() * 80 + 10 + '%';
                gift.style.animationDelay = Math.random() * 6 + 's';
                gift.style.animationDuration = (Math.random() * 3 + 5) + 's';
                document.body.appendChild(gift);
            }

            // Create hearts
            for (let i = 0; i < 10; i++) {
                const heart = document.createElement('div');
                heart.className = 'bg-decoration heart';
                heart.style.left = Math.random() * 100 + '%';
                heart.style.top = Math.random() * 100 + '%';
                heart.style.animationDelay = Math.random() * 2 + 's';
                heart.style.animationDuration = (Math.random() * 1 + 1.5) + 's';
                document.body.appendChild(heart);
            }
        }
        function createParticles() {
            for (let i = 0; i < 80; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.width = Math.random() * 15 + 8 + 'px';
                particle.style.height = particle.style.width;
                particle.style.animationDelay = Math.random() * 8 + 's';
                particle.style.animationDuration = (Math.random() * 4 + 4) + 's';
                
                // Add some particles with special shapes
                if (i % 10 === 0) {
                    particle.style.borderRadius = '0%';
                    particle.style.transform = 'rotate(45deg)';
                }
                if (i % 15 === 0) {
                    particle.style.borderRadius = '50% 0';
                }
                
                document.body.appendChild(particle);
            }
        }

        // Create enhanced fireworks effect
        function createFirework(x, y, type = 'burst') {
            const colors = ['#ff6b6b', '#feca57', '#48dbfb', '#ff9ff3', '#54a0ff', '#5f27cd', '#00d2d3', '#ff9f43'];
            
            if (type === 'mega') {
                // Create mega firework with trails
                for (let i = 0; i < 40; i++) {
                    const firework = document.createElement('div');
                    firework.className = 'firework-burst';
                    firework.style.left = x + 'px';
                    firework.style.top = y + 'px';
                    
                    const color = colors[Math.random() * colors.length | 0];
                    const angle = (i / 40) * Math.PI * 2;
                    const distance = Math.random() * 200 + 100;
                    
                    firework.style.background = `radial-gradient(circle, ${color}, transparent)`;
                    firework.style.transform = `translate(${Math.cos(angle) * distance}px, ${Math.sin(angle) * distance}px)`;
                    firework.style.animationDelay = Math.random() * 0.5 + 's';
                    
                    document.body.appendChild(firework);
                    
                    setTimeout(() => {
                        if (document.body.contains(firework)) {
                            document.body.removeChild(firework);
                        }
                    }, 3000);
                }
                
                // Add trailing sparks
                for (let i = 0; i < 15; i++) {
                    setTimeout(() => {
                        const trail = document.createElement('div');
                        trail.className = 'firework-trail';
                        trail.style.left = (x + Math.random() * 100 - 50) + 'px';
                        trail.style.top = y + 'px';
                        trail.style.background = `linear-gradient(to top, ${colors[Math.random() * colors.length | 0]}, transparent)`;
                        
                        document.body.appendChild(trail);
                        
                        setTimeout(() => {
                            if (document.body.contains(trail)) {
                                document.body.removeChild(trail);
                            }
                        }, 1500);
                    }, i * 100);
                }
            } else {
                // Regular firework burst
                for (let i = 0; i < 25; i++) {
                    const firework = document.createElement('div');
                    firework.className = 'fireworks';
                    firework.style.left = x + 'px';
                    firework.style.top = y + 'px';
                    firework.style.animationDelay = Math.random() * 0.5 + 's';
                    
                    const color = colors[Math.random() * colors.length | 0];
                    firework.style.background = `radial-gradient(circle, ${color}, transparent)`;
                    
                    document.body.appendChild(firework);
                    
                    setTimeout(() => {
                        if (document.body.contains(firework)) {
                            document.body.removeChild(firework);
                        }
                    }, 2000);
                }
            }
        }

        // Countdown timer
        function updateCountdown() {
            const targetDate = new Date('July 25, 2025 00:00:00').getTime();
            const now = new Date().getTime();
            const distance = targetDate - now;

            if (distance < 0) {
                // Birthday has arrived!
                document.getElementById('timerContainer').style.display = 'none';
                document.querySelector('.birthday-message').style.display = 'none';
                document.getElementById('celebrationStarted').style.display = 'block';
                
                // Create spectacular fireworks show
                const fireworksShow = () => {
                    // Multiple mega fireworks
                    for (let i = 0; i < 5; i++) {
                        setTimeout(() => {
                            const x = Math.random() * (window.innerWidth - 200) + 100;
                            const y = Math.random() * (window.innerHeight - 200) + 100;
                            createFirework(x, y, 'mega');
                        }, i * 300);
                    }
                    
                    // Regular fireworks scattered
                    for (let i = 0; i < 8; i++) {
                        setTimeout(() => {
                            const x = Math.random() * window.innerWidth;
                            const y = Math.random() * window.innerHeight;
                            createFirework(x, y, 'burst');
                        }, i * 150 + 1000);
                    }
                };
                
                // Start initial fireworks show
                fireworksShow();
                
                // Continue with regular fireworks every 2 seconds
                setInterval(() => {
                    // Random mega fireworks
                    if (Math.random() < 0.3) {
                        const x = Math.random() * (window.innerWidth - 200) + 100;
                        const y = Math.random() * (window.innerHeight - 200) + 100;
                        createFirework(x, y, 'mega');
                    }
                    
                    // Regular fireworks
                    const x = Math.random() * window.innerWidth;
                    const y = Math.random() * window.innerHeight;
                    createFirework(x, y, 'burst');
                }, 2000);
                
                // Extra spectacular show every 10 seconds
                setInterval(fireworksShow, 10000);
                
                return;
            }

            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);

            document.getElementById('days').textContent = days.toString().padStart(2, '0');
            document.getElementById('hours').textContent = hours.toString().padStart(2, '0');
            document.getElementById('minutes').textContent = minutes.toString().padStart(2, '0');
            document.getElementById('seconds').textContent = seconds.toString().padStart(2, '0');
        }

        // Random fireworks during countdown
        function randomFireworks() {
            if (Math.random() < 0.1) { // 10% chance every interval
                const x = Math.random() * window.innerWidth;
                const y = Math.random() * window.innerHeight;
                createFirework(x, y);
            }
        }

        // Initialize
        createDecorations();
        createParticles();
        updateCountdown();
        setInterval(updateCountdown, 1000);
        setInterval(randomFireworks, 3000);

        // Add click interaction for fireworks
        document.addEventListener('click', (e) => {
            createFirework(e.clientX, e.clientY);
        });
    </script>
</body>
</html>
